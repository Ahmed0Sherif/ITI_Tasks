
#include <stdio.h>
#include <math.h>
void main ()
{
	// float a, b, c;
	// float discriminator;
	
	// printf("Enter the coefficients:");
	// scanf("%f%f%f",&a,&b,&c);
	
	// discriminator = pow(b,2) - 4*a*c);
	
	// if (discriminator > 0)
		// printf ("Solutions are: %f and %f", -b + (sqrt(discriminator))/2*a );
	// else if (discri)
	int i;
	 
	for (i = 1; i <=3; ++i)
	{
		printf("E\n");
	}
	
}